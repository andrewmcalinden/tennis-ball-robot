bb This example shows ho to: aa
A) run plan php aps
B) use angularJS to call php mySal request and parse it on clinet side
For B) to work the mySql DB must be initialized on the server and populated.
The scripts are in setDb/ directory

To execute this code insde browser do:

sudo ln -s /home/balewski/malina/php-angularJS /var/www/html/janPhp2

Point browser to 92.168.144.216/janPhp2

All is based on this example:
http://www.phpro.org/tutorials/Consume-Json-Results-From-PHP-MySQL-API-With-Angularjs-And-PDO.html

Another example of Angular using JS require(), needs to be 'compiled' with
on server: browserify onServ1.js -o bundle.js
in browser URL=http://192.168.144.220/janPhp2/angUseBundle.html

Not all examples work. 
Only onServ1.js example works - it prints [1, 2, 3, 4, 5, 6] to Browser console 
(enable it via  View->developer ->developer Tools )

Example   onServ2.js has run-time error:
Uncaught TypeError: dns.lookup is not a function

Example   onServ3.js has run-time error:
Uncaught TypeError: exec is not a function